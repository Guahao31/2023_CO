pages:
  tags:
    - pages-builder
  stage: deploy
  script:
    - pip install mkdocs-heti-plugin 
    - dummy
  variables:
    BUILDER: 'mkdocs'
    WORKING_DIR: ''
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
